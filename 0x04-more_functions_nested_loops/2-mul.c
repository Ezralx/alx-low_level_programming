#include "main.h"
/**
 * mul -a function that multiplies two integers.
 * @a: the first intger
 * @b: the second intger
 * Return: the result of mltiplication of two intigers
 */
int mul(int a, int b)
{
	return (a * b);

}

