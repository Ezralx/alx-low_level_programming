#include "main.h"
/**
 * _abs - function that computes absolute value of an integer.
 *@v: the value
 * Return:the absolute value of a given number
 */
int _abs(int v)
{
	if (v >= 0)
	{
		return (v);
	}
	else
	{
		return (-v);
	}
}

